class CartItem {
  constructor(quantity, productPrice, productTitle, sum) {
    (this.quantity = quantity),
      (this.productPrice = productPrice),
      (this.productPrice = productTitle);
    this.sum = sum;
  }
}

export default CartItem;
